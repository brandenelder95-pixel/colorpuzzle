// Web / Expo Go stub — on native, appOpenAd.native.ts is used instead.
// No-op: app-open ads are a native-only AdMob format.
// eslint-disable-next-line @typescript-eslint/require-await
export async function loadAndShowAppOpenAd(_adsEnabled: boolean, _isInGame?: boolean): Promise<void> {
  // nothing to do on web or Expo Go
}
